/* SYNTHSPACE AUDIO LAYER - https://synthspace.rocks
 * Copyright 2020 Bright Light Interstellar Limited - All rights reserved. */

namespace Songspace.Container {
	public class ContainerSlotGeneric<T> : ContainerSlot {
		
		//[SerializeField] private new ContainerGeneric<T> m_CurrentContainer;
		// private readonly Queue<ContainerGeneric<T>> m_ContainerQueue = new Queue<ContainerGeneric<T>>();

		// public string Description;
		

		// public void AddContainer( ContainerGeneric<T> container ) {
		// 	m_ContainerQueue.Enqueue( container );
		// }
		//
		// public void SetCurrentContainer( ContainerGeneric<T> container ) {
		// 	m_CurrentContainer = container;
		// 	base.SetCurrentContainer( container );
		// 	Debug.Log( "New Container " + CurrentContainer );
		// 	onNewCurrentContainer?.Invoke();
		// }

		// void Update() {
		// 	if ( isNew ) {
		// 		isNew = false;
		// 		SetCurrentContainer( CurrentContainer );
		// 	}
		// }

	}
}
